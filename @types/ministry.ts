export type Ministry = {
  id: string;
  title: string;
  description: string;
  date?: string;
  place?: string;
  schedules?: string;
  phone?: string;
  email?: string;
  address?: string;
  contactAvatarUrl?: string;
  contactName?: string;
  imageUrl?: string;
  downloadUrl?: string;
};
