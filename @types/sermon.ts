// TO DO: review the data scope and types later w Rev.
export type Sermon = {
  id: string;
  title: string;
  description: string;
  date: string;
  videoUrl: string;
  imageUrl?: string;
  downloadUrl?: string;
};
